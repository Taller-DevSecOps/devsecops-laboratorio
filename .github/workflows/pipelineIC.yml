#Pipeline IC - DevSecOps Tarea Final (Desarrollado por Sandra)
name: WorkFlow smedels - DevSecOps

on:
  push:
    branches:
      - feat-devsecops

jobs:
   Pipeline:
    runs-on: ubuntu-latest
    steps:
      # 1. Clonar de repositorio
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Repositorio clonado exitosamente
        run: |
          echo "::notice title=Clonación OK::Repositorio ${{ github.repository }} clonado correctamente"
          echo "Iniciando pipeline DevSecOps para el proyecto ${{ github.event.repository.name }}"

